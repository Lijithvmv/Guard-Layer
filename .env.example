# The core heuristic engine needs no configuration or keys.
# These are for optional detectors on the roadmap (embeddings / LLM-based checks).

# OPENAI_API_KEY=
# GUARDLAYER_FLAG_THRESHOLD=0.4
# GUARDLAYER_BLOCK_THRESHOLD=0.8
